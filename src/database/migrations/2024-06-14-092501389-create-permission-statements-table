-- Up

CREATE TABLE IF NOT EXISTS `permission_statements`
  (
    `id` INT UNSIGNED AUTO_INCREMENT NOT NULL,
    `uuid` BINARY(16) NOT NULL,
    `permission_id` INT UNSIGNED NOT NULL,
    `role_id` INT UNSIGNED NOT NULL,
    `created_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    CONSTRAINT `permission_statements_id_primary_key` PRIMARY KEY (`id`),
    CONSTRAINT `permission_statements_uuid_unique_key` UNIQUE (`uuid`)
  ) ENGINE=InnoDB AUTO_INCREMENT=1 DEFAULT CHARSET=utf8;

--- // ---

-- Down

DROP TABLE IF EXISTS `permission_statements`;
